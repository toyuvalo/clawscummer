# ClawsCummer — project context for Claude Code

> Auto-loaded when you start Claude in `E:\clawscummer` (via `dev clawscummer`).
> The **vault is the single source of truth** — this file only pins entry points.

## Read first
1. `E:\Claude\index.md` — master map
2. `E:\Claude\CURRENT.md` — live state / blockers
3. `E:\Claude\wiki\projects\clawscummer.md` → [[clawscummer]] (and [[clawscummer-web-portal]])

## This repo
- Path: `E:\clawscummer` — pywebview GUI wrapping the Claude/Gemini CLIs
- Launch with `dev clawscummer` (per-project session, not the system32 catch-all)
- Credentials in **OneCLI** — never inline tokens in commands.

<!-- Repo-specific build/deploy/test/gotchas — fill in as discovered. -->
